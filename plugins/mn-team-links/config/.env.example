# OPTIONAL — only for the headless / cron fallback (no MCP available).
# Normal assistant use needs NONE of this: the skill reads n8n through the
# connected n8n MCP and stores no secret.
N8N_API_URL=https://n8n.speedandfunction.com/api/v1
N8N_API_KEY=<paste-n8n-api-key-here-only-for-headless-use>

# Optional overrides (defaults shown):
# MN_WORKFLOW_ID=SkEnOMQyotDXM05Z
# MN_CONFIG_NODE=Config + Classification
