.env
*.env
!*.env.example
node_modules/
secrets/
*.key
*-sa.json
*.after.js
backups/
.tmp/
**/.tmp/
.DS_Store
.planning/
*.local

# ai-digest skill — local digest output written to the cwd (internal ClickUp pools +
# citations always; raw per-person Geekbot standup answers when Geekbot is enabled).
# Never commit it.
ai-digest-*.md
ai-digest-runs/
